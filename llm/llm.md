# 简介
LLM 的相关知识

## KV Cache
大语言模型生成文本时候, 为了加速推理、避免重复计算引入的"短期记忆"机制. 在 LLM 生成文本是自回归的, 会一个字一个字往外蹦. 模型要生成 100 个字时, 需要看到并理解前面 99 个字. 有 KV Cache 后, 模型不需要每生成一个字都把前面的字重新计算. 通过 KV Cache, 模型可以将前面的已经计算好的特征向量直接保存在显存里面, 生成新的文字时候, 只需计算新字本身特征, 然后去显存读取前面字的特征进行拼接即可.

##  